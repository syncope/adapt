! multiple gaussian / multiple peak fitting needs interaction

! change pdf plotting to subplotting: 3x4 -> 12 plots per page

! more output to the command line/call window

! background subtraction -> normalization

! add/mark the points that are moved by the despiking routine
